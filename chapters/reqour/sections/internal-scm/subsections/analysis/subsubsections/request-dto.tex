\documentclass[../main.tex]{subfiles}

\begin{document}

Internal SCM repository creation request DTO is JSON of the following format:

\begin{lstlisting}[numbers=none]
{
  "project": "string",
  "taskId": "string",
  "callback": {
    "method": "string",
    "uri": "string",
    "headers": [
      {
        "name": "string",
        "value": "string"
      }
    ]
  }
}
\end{lstlisting}

%% TODO: describle the fields

\end{document}
